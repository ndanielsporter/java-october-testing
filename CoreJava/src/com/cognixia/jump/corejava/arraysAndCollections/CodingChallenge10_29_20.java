package com.cognixia.jump.corejava.arraysAndCollections;

public class CodingChallenge10_29_20 {
	public static void main(String[] args) {
		
		String helloWorld = "Hello World";
		
		// Create a method that takes a string, reverses it, and returns the reversed string
		// - you can use a built in method to do this
		
		// Create a second method that does not use any built in methods to reverse the string
		// Steps:
		// loop through string
		
		String reverseString = reverseString(helloWorld);
		
		public static String reverseString(String inputString) {
			int inputStringLength = inputString.length();
			
			for (char i = inputStringLength; i <= [0]; i--) {
				
			}
		// return in reversed order
		
		// For a given string with multiple words, reverse each word individually
		
		}
	}
}
